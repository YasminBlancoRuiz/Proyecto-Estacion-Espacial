import { FiltroNavePipe } from './filtro-nave.pipe';

describe('FiltroNavePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroNavePipe();
    expect(pipe).toBeTruthy();
  });
});
